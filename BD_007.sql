CREATE TABLE Produto(
  nome VARCHAR(80),
  tipo VARCHAR(20),
  designação genérica VARCHAR(30),
  marca VARCHAR(30),
  mercado VARCHAR(10),
  numero identificação INT(10)
);

CREATE TABLE Empresa(
  morada sede VARCHAR(100),
  nif INT(9),
  capital social INT(20)
);

CREATE TABLE Contactos ECOFCUL(

);
